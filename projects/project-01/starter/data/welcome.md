This app shows a list of documents stored in the data directory.

Select a document on the left to read its contents in the viewer.

Use the question box below the viewer to search across all documents. The app looks for paragraphs that share words with your question and shows the closest matches as answers.
