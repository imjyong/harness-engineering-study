The data directory holds the documents that the app reads. Add or remove .md or .txt files here and they will appear in the document list automatically.

Electron apps run a main process and one or more renderer processes. The main process creates windows and has access to the file system. The renderer process shows the UI and talks to the main process through a preload script.
